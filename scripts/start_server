#!/bin/bash

# Use the TEST_PORT environment variable if provided, otherwise default to 8080
PORT=${TEST_PORT:-8080}

# Update Apache configuration to listen on the dynamically set port
sed -i "s/Listen 80/Listen ${PORT}/" /etc/httpd/conf/httpd.conf

# Restart Apache to apply the new configuration
systemctl restart httpd